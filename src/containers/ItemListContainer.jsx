import React from 'react'

function ItemListContainer({ greeting }) {
    const[ , ] = usestate([]);

    useEffect(() => {
        fetch()
       
        .then()

        .catch()

        .finally()

    return (
        <div>
            { greeting }
        </div>
    )
}

export default ItemListContainer;
